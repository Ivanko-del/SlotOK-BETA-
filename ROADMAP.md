# SlotOK — черга робіт

Великі напрями, які ще не зроблені. Кожен пункт — окремий шматок роботи зі
своїм входом у код, щоб не витрачати сесію на повторне розслідування.

Порядок у файлі — за співвідношенням «користь / вартість», а не за бажанням.
Коли беремося за пункт — пишемо тут «В РОБОТІ», коли готово — виносимо в
`BUILTIN_CHANGELOG` (див. `CLAUDE.md`) і прибираємо звідси.

---

## 1. Спорт-ставки, які справді платять

**Стан:** матчі справжні — `loadRealSportMatches()` (`app.js`) тягне ESPN
scoreboard API, `parseESPNScoreboard()` розбирає, `FALLBACK_MATCHES` підстраховує.
Фінальні рахунки завершених матчів тепер пишуться в `sport_matches/<id>`
(`publishFinalScores()`), і `checkPendingBetsOnStartup()` закриває ставку за
справжнім рахунком; симуляція лишилась запасним варіантом через 3 години й
чесно підписана в сповіщенні.

**Чого немає:** розрахунок відбувається лише тоді, коли гравець сам відкрив
застосунок (і лише над власними ставками) — закритий вкладкою акаунт не
отримує виплату, доки не зайде. Виплата йде на `users/<u>/balance`, а не на
картку, з якої ставили.

**Що робити:** серверний розрахунок, бо клієнт не можна питати «хто виграв».
- `api/settle-sports.js` — читає відкриті ставки, добирає фінальні рахунки з
  ESPN за `match.id`, закриває виграшні/програшні, нараховує виплату.
- Виплата має йти на картку, з якої ставили: `cardBalancePath(u, cardId)`,
  той самий шлях, що для депозиту й повернення виводу. Ставку теж треба
  штампувати `cardId` при прийомі (як це вже зроблено для `withdraw_requests`).
- Vercel Cron (`vercel.json` → `crons`) раз на 15–30 хв.
- Ідемпотентність обов'язкова: cron може спрацювати двічі, ставка не має
  оплатитись двічі. Прапорець `settled` + перевірка перед нарахуванням.

**Розмір:** одна повна сесія.

---

## 2. Покер як справжній

**Стан:** поточна реалізація спрощена.

**Що робити:** один рушій Техаського холдему — банк і сайд-поти, блайнди,
черга ходів, визначення комбінацій, шоудаун. Рушій має бути чистою функцією
над станом столу, щоб його можна було накрити selfcheck-ом (комбінації й
сайд-поти — саме те, що тихо ламається).

**Розмір:** одна-дві сесії. Найбільша користь — не UI, а коректний рушій.

---

## 3. Мультиплеєр: полагодити або переписати

**Стан:** `mp_rooms` у Firebase, `loadMpRooms()`, `mpRoomId`/`mpIsHost`
(`app.js` ~10855–11250). Кімнати монополії.

**Що робити:** спершу з'ясувати, що саме ламається (хост вийшов? гонка при
приєднанні? стан не сходиться між гравцями?) — і лише потім вирішувати,
лагодити чи переписувати. Переписування без діагнозу поверне ті самі баги.

**Розмір:** півсесії на діагноз, далі за результатом.

---

## 4. Редизайн ігор

**Стан:** у застосунку три візуальні мови — `ks-*` (Каса, Профіль, Сповіщення,
Налаштування, «Ще», усі повноекранні вікна), `lux-*` (Головна) і стара казинова
(лоббі та ігрові екрани).

**Що робити:** доводити `ks-*` екран за екраном. Місток уже є (`style.css`,
блок «МІСТ ЗІ СТАРОЇ КАЗИНОВОЇ МОВИ В МОВУ КАСИ») — щоб перевести екран,
достатньо додати йому `class="ledger"` і зняти інлайнові кольори, як це
зроблено для вкладки «Ще». Ігрові екрани складніші: там багато власної
розмітки, не лише кнопок.

**Порядок за помітністю:** лоббі → слоти → crash/mines/plinko → решта.

**Розмір:** по пів сесії на групу екранів. Не робиться «все одразу».

---

## 5. Глобальний чат

**Що робити:** реакції, відповіді на повідомлення, згадки (@нік) зі
сповіщенням, антиспам (тротлінг на клієнті + правило в базі), модерація для
адміна прямо з чату.

**Розмір:** півсесії.

---

## 6. Телеграм-бот

**Стан:** `api/telegram-webhook.js` (~650 рядків), `lib/telegram*.js`.
`scripts/selfcheck-telegram-bot.js` уже пінить `/cancel`, `/top`, `/vip`,
`/ref`, `/promo`.

**Що робити:** зібрати конкретні баги (зараз списку немає — потрібні приклади
від гравців), додати команди. Кожну нову команду накривати тим самим
selfcheck-ом, бо бот тестується лише ним.

**Розмір:** півсесії, але спершу потрібен список реальних скарг.

---

## 7. Більше ігор

Безрозмірний пункт. Має сенс лише після того, як наявні ігри перестануть
містити баги — інакше множимо проблеми.

---

## 8. FCM push — лишилось два ключі власника

Код готовий і чекає (`app.js` `registerFcmToken()`, `lib/fcm.js`, `sw.js`).
Потрібні два значення, які може створити лише власник проєкту:

1. **`FCM_VAPID_KEY`** в `app.js` — публічний ключ, Firebase Console →
   Project Settings → Cloud Messaging → Web Push certificates. Вставляється
   прямо в код.
2. **`FIREBASE_SERVICE_ACCOUNT_KEY`** — справжній секрет. Firebase Console →
   Project Settings → Service Accounts → Generate new private key → увесь JSON
   як змінна середовища у Vercel (Production). **Ніколи не комітити і не
   надсилати в чат.**

Доки їх немає, push тихо не працюють, решта сайту — як зазвичай.

---

## 9. Борг, про який варто пам'ятати

- **Футер обіцяє «Реальних грошей немає», а Каса приймає поповнення й робить
  виводи на банківські картки.** Це пряме протиріччя в текстах сайту. Перш ніж
  писати будь-яку юридичну сторінку, треба вирішити, що з цього правда.
- **Вікове обмеження 14+** нижче за те, що дозволяють закони більшості
  юрисдикцій для платформ зі ставками. Це рішення власника; тут воно записане,
  щоб не загубилось.
- **Модель загроз відкритої бази** прийнята свідомо: правила Firebase дозволяють
  клієнту писати в свої вузли, тож усі перевірки на клієнті — це зручність,
  а не захист.
- **Права адмінів** (`users/<нік>/adminPerms`, вкладка 👑 Адміни) з цієї ж
  причини керують лише тим, що адмін бачить і може натиснути в панелі, плюс
  лишають слід у `admin_action_log`. Обмежений адмін із консоллю браузера
  все одно може писати в базу напряму. Щоб зробити це справжнім розмежуванням,
  потрібен той самий серверний шар, що й для решти пунктів вище: записи в
  `users/*`, `site_config/*` і підтвердження заявок мають іти через сервер,
  який сам перевіряє `adminPerms`.
